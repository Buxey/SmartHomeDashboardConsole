from Room import Room


def main():
    number_of_rooms = input("How many rooms are there in this property?: ")
    number_of_smartplugs = input("How many plugs do you want to place in this property?: ")







    # names = ["Jack", "Connor"]
    # for index, name in enumerate(names):
    #     print(index, name)
    #   0 jack 1 connor

main()
